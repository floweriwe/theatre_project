# 🎭 Theatre Management System

**Версия**: 0.1.1  
**Дата**: 26 декабря 2025

Система управления театром — MVP для автоматизации документооборота, инвентаризации, управления спектаклями и расписанием.

---

## 🚀 Быстрый старт

### 1. Распаковка и запуск

```bash
# Распаковать архив
unzip theatre-management-system.zip
cd theatre-management-system

# Сделать скрипты исполняемыми
chmod +x scripts/*.sh

# Запустить полную инициализацию
./scripts/init_all.sh
```

### 2. Доступ к системе

| Сервис | URL |
|--------|-----|
| 🌐 Frontend | http://localhost:5173 |
| 🔧 Backend API | http://localhost:8000 |
| 📚 API Docs | http://localhost:8000/docs |

### 3. Тестовые данные

```
👤 Логин:  admin@theatre.test
🔑 Пароль: Theatre2024!
```

---

## 📋 Скрипты

| Скрипт | Описание |
|--------|----------|
| `scripts/init_all.sh` | Полная инициализация (Docker, миграции, seed data, файлы) |
| `scripts/test_all.sh` | Полное тестирование всех API endpoints |

### Запуск инициализации

```bash
./scripts/init_all.sh
```

Выполняет:
- ✅ Запуск Docker контейнеров
- ✅ Ожидание готовности сервисов
- ✅ Применение миграций БД
- ✅ Генерацию seed data (пользователи, инвентарь, документы, спектакли, расписание)
- ✅ Создание тестовых файлов (PDF документы, постеры)

### Запуск тестирования

```bash
./scripts/test_all.sh
```

Проверяет:
- ✅ Доступность сервисов (Backend, Frontend, Redis, PostgreSQL)
- ✅ Аутентификацию (login, JWT токены)
- ✅ Все API endpoints (GET, POST, PATCH, DELETE)
- ✅ CRUD операции
- ✅ Файлы в storage

### Диагностика внутри контейнера

```bash
docker-compose -f docker-compose.dev.yml exec backend python -m scripts.diagnostic
```

---

## 🏗️ Архитектура

### Технологии

| Слой | Технологии |
|------|------------|
| Backend | Python 3.12, FastAPI, SQLAlchemy 2.0, Alembic, Pydantic 2.0 |
| Frontend | React 18, TypeScript 5, Vite 5, Tailwind CSS 3, Zustand |
| База данных | PostgreSQL 16 |
| Кэш | Redis 7 |
| Инфраструктура | Docker, Docker Compose, Nginx |

### Структура

```
theatre-management-system/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # API роутеры (auth, inventory, documents, performances, schedule)
│   │   ├── models/          # SQLAlchemy модели
│   │   ├── schemas/         # Pydantic схемы
│   │   ├── services/        # Бизнес-логика
│   │   ├── repositories/    # Repository pattern
│   │   └── core/            # Security, permissions, exceptions
│   ├── alembic/             # Миграции БД
│   └── scripts/             # init_db.py, diagnostic.py
│
├── frontend/
│   └── src/
│       ├── components/      # UI компоненты (Button, Card, Modal, etc.)
│       ├── pages/           # Страницы (25 шт.)
│       ├── services/        # API клиенты (axios)
│       ├── types/           # TypeScript типы
│       └── store/           # Zustand stores
│
├── scripts/
│   ├── init_all.sh          # Полная инициализация
│   └── test_all.sh          # Полное тестирование
│
├── docker-compose.dev.yml   # Development
└── docker-compose.yml       # Base
```

---

## 📦 Модули

### 1. Инвентарь (`/inventory`)
- Каталог реквизита, костюмов, оборудования
- Категории и места хранения
- Резервирование для спектаклей
- История перемещений

### 2. Документы (`/documents`)
- Каталог документов с категориями и тегами
- Версионирование файлов (N+1)
- Привязка к спектаклям
- PDF, DOCX, изображения

### 3. Спектакли (`/performances`)
- Паспорт спектакля (секции)
- Жизненный цикл: подготовка → репертуар → архив
- Связь с инвентарём и документами
- Постеры

### 4. Расписание (`/schedule`)
- Календарь событий
- Типы: спектакль, репетиция, техническая репетиция, генеральная, собрание
- Участники и статусы
- Фильтрация по периоду

---

## 🔧 Разработка

### Команды Docker

```bash
# Запуск
docker-compose -f docker-compose.dev.yml up -d

# Логи
docker-compose -f docker-compose.dev.yml logs -f backend
docker-compose -f docker-compose.dev.yml logs -f frontend

# Остановка
docker-compose -f docker-compose.dev.yml down

# Полная очистка
docker-compose -f docker-compose.dev.yml down -v
```

### Миграции

```bash
# Создать миграцию
docker-compose exec backend alembic revision --autogenerate -m "описание"

# Применить
docker-compose exec backend alembic upgrade head

# Откатить
docker-compose exec backend alembic downgrade -1
```

---

## 🐛 Решение проблем

### Контейнеры не запускаются

```bash
docker-compose -f docker-compose.dev.yml down -v
docker-compose -f docker-compose.dev.yml up -d --build
```

### Ошибки БД

```bash
docker-compose exec backend alembic upgrade head
docker-compose exec backend python -m scripts.init_db
```

### Frontend не загружается

```bash
docker-compose -f docker-compose.dev.yml up -d --build frontend
```

---

## 📝 Changelog

### v0.1.1 (26.12.2025)
- ✅ Исправлены критические баги в API converters (metadata → extra_data)
- ✅ Исправлены вызовы методов в PerformanceViewPage
- ✅ Переписан schedule_service для работы с реальным API
- ✅ Добавлены скрипты init_all.sh и test_all.sh
- ✅ Добавлена генерация PDF и постеров
- ✅ Добавлена диагностика (diagnostic.py)

### v0.1.0 (25.12.2025)
- 🎉 Первая версия MVP
- ✅ 4 модуля: Инвентарь, Документы, Спектакли, Расписание
- ✅ JWT аутентификация
- ✅ Docker Compose setup
- ✅ Дизайн-система "Modern Theatre Elegance"

---

## 📜 Лицензия

MIT License © 2025
