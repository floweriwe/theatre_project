# 🎭 Theatre Management System — Статус проекта

**Дата**: 26 декабря 2025  
**Версия**: 0.1.0 MVP  
**Фаза**: Phase 4b — API Integration (частично завершена)

---

## 📊 Общая статистика

| Метрика | Значение |
|---------|----------|
| Python файлов (backend) | 65 |
| TypeScript файлов (frontend) | 84 |
| API endpoints | 79 |
| Моделей БД | 6 |
| Страниц UI | 25 |
| UI компонентов | 13 |
| Всего файлов кода | 150 |

---

## ✅ Что реализовано и работает

### Backend (FastAPI)
- [x] **Аутентификация**: JWT + Refresh tokens, login/logout/register/refresh
- [x] **Модели БД**: User, Theater, InventoryItem, Document, Performance, ScheduleEvent
- [x] **Миграции**: Alembic с 5 миграциями (initial, inventory, performances, documents, schedule)
- [x] **API endpoints**: CRUD для всех 4 модулей
- [x] **Redis**: Кэширование, blacklist токенов
- [x] **CORS**: Настроен для localhost:5173
- [x] **Seed Data**: init_db.py генерирует полный набор тестовых данных

### Frontend (React + TypeScript)
- [x] **Дизайн-система**: Dark theme "Modern Theatre Elegance"
- [x] **Авторизация**: Login/Register с Zustand persist
- [x] **Dashboard**: Статистика из API, профиль пользователя
- [x] **Списки**: Inventory, Documents, Performances — загрузка и отображение
- [x] **UI компоненты**: Button, Card, Input, Modal, Badge, Select, Table, Tabs, etc.
- [x] **Layout**: Sidebar, Header, MainLayout с навигацией
- [x] **Роутинг**: React Router v6, protected routes

### Infrastructure
- [x] **Docker Compose**: db, redis, backend, frontend
- [x] **Volumes**: postgres_data, redis_data, storage
- [x] **Health checks**: Все сервисы

---

## ⚠️ Проблемы и баги

### Критические (блокируют работу)

| # | Проблема | Страница | Причина |
|---|----------|----------|---------|
| 1 | "Ошибка загрузки данных" | `/inventory/:id` | API возвращает 404 или несовместимый формат |
| 2 | "Ошибка загрузки данных" | `/documents/:id` | API возвращает 404 или несовместимый формат |
| 3 | "Ошибка загрузки данных" | `/performances/:id` | API возвращает 404 или несовместимый формат |
| 4 | "Не удалось загрузить расписание" | `/schedule` | API endpoint или формат данных |

### UI/UX проблемы

| # | Проблема | Локация |
|---|----------|---------|
| 1 | Мелкие надписи секций | Sidebar: ГЛАВНОЕ, УПРАВЛЕНИЕ, ОТЧЁТЫ |
| 2 | Обрезанные заголовки | Плитки инвентаря (названия не вмещаются) |
| 3 | Обрезанные заголовки | Плитки документов (названия не вмещаются) |
| 4 | Нет preview документов | DocumentViewPage |
| 5 | Нет детальной карточки | InventoryItemPage |
| 6 | Нет детальной карточки | PerformanceViewPage |

---

## 🚧 Не реализовано (требуется доработка)

### Модуль Инвентарь
- [ ] Детальная карточка предмета с полной информацией
- [ ] Перемещение между локациями
- [ ] История движений
- [ ] Бронирование для спектаклей
- [ ] Фильтры: по категории, статусу, локации, спектаклю
- [ ] Поиск с автокомплитом
- [ ] Массовые операции (выбор нескольких)
- [ ] Экспорт отчёта (Excel/PDF)
- [ ] Привязка фото

### Модуль Документы
- [ ] Просмотр/Preview документа
- [ ] Скачивание файла
- [ ] Редактирование метаданных
- [ ] Версионирование
- [ ] Привязка к спектаклю
- [ ] Категории и теги
- [ ] Фильтры: по типу, категории, спектаклю, дате

### Модуль Спектакли
- [ ] Детальная карточка с секциями
- [ ] Паспорт спектакля (документация)
- [ ] Привязанный инвентарь (реквизит, костюмы)
- [ ] Расписание репетиций и показов
- [ ] Назначение актёров (Cast)
- [ ] Статус жизненного цикла
- [ ] Бюджет и расходы

### Модуль Расписание
- [ ] Загрузка и отображение событий
- [ ] Создание/редактирование событий
- [ ] Drag & drop в календаре
- [ ] Фильтры: по типу, спектаклю, площадке
- [ ] Проверка конфликтов
- [ ] Уведомления

### Общие функции
- [ ] Пользователи: список, редактирование, роли
- [ ] Настройки: категории, локации, площадки
- [ ] Аналитика/Отчёты
- [ ] Уведомления (bell icon работает, но нет данных)
- [ ] Поиск глобальный

---

## 🔄 API — Соответствие Frontend ↔ Backend

### Inventory

| Frontend метод | Backend endpoint | Статус |
|----------------|------------------|--------|
| `getItems()` | `GET /inventory/` | ✅ Работает |
| `getItem(id)` | `GET /inventory/{id}` | ❌ Ошибка |
| `createItem()` | `POST /inventory/` | ⚠️ Не тестировалось |
| `updateItem()` | `PATCH /inventory/{id}` | ⚠️ Не тестировалось |
| `deleteItem()` | `DELETE /inventory/{id}` | ⚠️ Не тестировалось |

### Documents

| Frontend метод | Backend endpoint | Статус |
|----------------|------------------|--------|
| `getDocuments()` | `GET /documents/` | ✅ Работает |
| `getDocument(id)` | `GET /documents/{id}` | ❌ Ошибка |
| `createDocument()` | `POST /documents/` | ⚠️ Не тестировалось |
| `updateDocument()` | `PATCH /documents/{id}` | ⚠️ Не тестировалось |
| `deleteDocument()` | `DELETE /documents/{id}` | ⚠️ Не тестировалось |

### Performances

| Frontend метод | Backend endpoint | Статус |
|----------------|------------------|--------|
| `getPerformances()` | `GET /performances/` | ✅ Работает |
| `getPerformance(id)` | `GET /performances/{id}` | ❌ Ошибка |
| `createPerformance()` | `POST /performances/` | ⚠️ Не тестировалось |
| `updatePerformance()` | `PATCH /performances/{id}` | ⚠️ Не тестировалось |
| `deletePerformance()` | `DELETE /performances/{id}` | ⚠️ Не тестировалось |

### Schedule

| Frontend метод | Backend endpoint | Статус |
|----------------|------------------|--------|
| `getEvents()` | `GET /schedule/` | ❌ Ошибка |
| `getEvent(id)` | `GET /schedule/{id}` | ⚠️ Не тестировалось |
| `createEvent()` | `POST /schedule/` | ⚠️ Не тестировалось |
| `updateEvent()` | `PATCH /schedule/{id}` | ⚠️ Не тестировалось |
| `deleteEvent()` | `DELETE /schedule/{id}` | ⚠️ Не тестировалось |

---

## 📁 Текущая структура (упрощённая)

```
theatre-management-system/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # 5 роутеров: auth, inventory, documents, performances, schedule
│   │   ├── models/          # 6 моделей: user, theater, inventory, document, performance, schedule
│   │   ├── schemas/         # Pydantic схемы
│   │   ├── services/        # Бизнес-логика
│   │   ├── repositories/    # Работа с БД
│   │   ├── core/            # Security, permissions, exceptions
│   │   └── database/        # Session, base
│   ├── scripts/
│   │   └── init_db.py       # Генерация seed data
│   └── alembic/versions/    # 5 миграций
│
├── frontend/
│   └── src/
│       ├── components/
│       │   ├── ui/          # 13 компонентов дизайн-системы
│       │   ├── layout/      # Sidebar, Header, MainLayout
│       │   └── features/    # EventFormModal, NotificationCenter
│       ├── pages/           # 25 страниц (12 директорий)
│       ├── services/        # API клиенты (6 файлов)
│       ├── types/           # TypeScript типы (7 файлов)
│       ├── store/           # Zustand (authStore)
│       ├── hooks/           # useAuth
│       └── utils/           # cn, constants, helpers
│
├── docker-compose.dev.yml
├── docker-compose.yml
└── docs/
```

---

## 🎯 Приоритеты следующего этапа

### P0 — Критические (блокеры)
1. Исправить загрузку детальных страниц (inventory, documents, performances)
2. Исправить загрузку расписания
3. Синхронизировать типы Frontend ↔ Backend

### P1 — Важные (core функционал)
1. Детальные карточки с полной информацией
2. CRUD операции (создание, редактирование, удаление)
3. Фильтры и поиск
4. Привязка инвентаря к спектаклям

### P2 — Улучшения UX
1. Увеличить размер секций в Sidebar
2. Улучшить карточки плиточного отображения
3. Добавить loading states везде
4. Добавить toast уведомления

### P3 — Дополнительный функционал
1. Экспорт отчётов
2. Аналитика
3. Уведомления
4. Глобальный поиск

---

## 🔐 Тестовые данные

**Админ**: admin@theatre.test / Theatre2024!

**Seed data (init_db.py)**:
- 7 пользователей
- 65 предметов инвентаря
- 53 документа
- 12 спектаклей
- 83 события расписания
- 5 категорий инвентаря
- 8 локаций
- 6 категорий документов
