# Theatre Design System — "Modern Theatre Elegance"

## 📋 Обзор

Дизайн-система Theatre MVP переработана с концепцией **"Modern Theatre Elegance"** — сочетание элегантности классического театра с современным минимализмом.

### Ключевые изменения

| Аспект | Было | Стало |
|--------|------|-------|
| **Основной фон** | Персиковый (#FCDEBE) | Светло-серый (#F8FAFC) |
| **Sidebar/Header** | Серо-фиолетовый (#5E5768) | Глубокий тёмно-синий (#0F1419) |
| **Карточки** | Оливковый (#D4D2A5) | Чистый белый (#FFFFFF) |
| **Акценты** | Отсутствовали | Тёплое золото (#D4A574) |
| **Типографика** | Inter (generic) | Cormorant Garamond + Inter |
| **Логотип** | Эмодзи 🎭 | Кастомный SVG |

---

## 🎨 Цветовая палитра

### Primary — Основной тёмный цвет
Используется для sidebar, header, важных элементов.

```css
--color-primary: #0F1419;
--color-primary-50: #F7F8F9;
--color-primary-100: #E8EBED;
--color-primary-500: #445261;
--color-primary-700: #1E2832;
--color-primary-900: #0F1419;
```

### Gold — Акцентный золотой
Ключевой акцент дизайна. Используется для кнопок, активных состояний, важных элементов.

```css
--color-gold: #D4A574;
--color-gold-50: #FDF8F3;
--color-gold-200: #F2D9BC;
--color-gold-400: #D4A574;
--color-gold-600: #A6693A;
```

### Surface — Поверхности
Для карточек и контейнеров.

```css
--color-surface: #FFFFFF;
--color-surface-100: #FAFBFC;
--color-surface-200: #F1F5F9;
--color-surface-300: #E2E8F0;
```

### Background — Фон приложения
```css
--color-background: #F8FAFC;
```

### Text — Цвета текста
```css
--text-primary: #1E293B;
--text-secondary: #64748B;
--text-muted: #94A3B8;
--text-inverse: #FFFFFF;
```

### Semantic Colors — Семантические цвета
```css
/* Success */
--color-success: #059669;
--color-success-light: #D1FAE5;

/* Warning */
--color-warning: #D97706;
--color-warning-light: #FEF3C7;

/* Error */
--color-error: #DC2626;
--color-error-light: #FEE2E2;

/* Info */
--color-info: #0284C7;
--color-info-light: #E0F2FE;
```

---

## 🔤 Типографика

### Шрифты

**Display (Заголовки):** Cormorant Garamond — элегантный serif шрифт с театральным характером.

**Body (Основной текст):** Inter — современный sans-serif для отличной читаемости.

**Mono (Код):** JetBrains Mono

### Размеры заголовков

```css
.text-display-xl { font-size: 3.5rem; line-height: 1.1; }
.text-display-lg { font-size: 2.5rem; line-height: 1.2; }
.text-display-md { font-size: 2rem; line-height: 1.3; }
.text-display-sm { font-size: 1.5rem; line-height: 1.4; }
```

### Применение

```jsx
<h1 className="font-display text-display-lg font-bold text-text-primary">
  Заголовок страницы
</h1>

<p className="font-sans text-base text-text-secondary">
  Основной текст параграфа
</p>
```

---

## 🧩 Компоненты

### Button (Кнопка)

**Варианты:**
- `primary` — золотая, основное действие
- `secondary` — тёмная
- `outline` — с обводкой
- `ghost` — прозрачная
- `danger` — красная, опасное действие
- `gold-outline` — элегантная золотая обводка

**Размеры:** `sm`, `md`, `lg`, `xl`

```jsx
<Button variant="primary" size="lg" leftIcon={<Save />}>
  Сохранить
</Button>

<Button variant="gold-outline">
  Премьера
</Button>
```

### Card (Карточка)

**Варианты:**
- `default` — стандартная с тенью
- `elevated` — приподнятая, больше тени
- `outlined` — только обводка
- `gold` — с золотым акцентом
- `interactive` — с hover эффектом

**Props:**
- `title` — заголовок
- `subtitle` — подзаголовок
- `icon` — иконка рядом с заголовком
- `action` — элемент в правом углу
- `accent` — золотая полоска сверху

```jsx
<Card 
  title="Спектакли" 
  subtitle="Репертуар театра"
  icon={<Theater />}
  accent
>
  <Content />
</Card>
```

### Input (Поле ввода)

**Варианты:**
- `default` — стандартное
- `filled` — с заливкой
- `dark` — для тёмных поверхностей

**Особенности:**
- Автоматический toggle для пароля
- Поддержка иконок слева/справа
- Валидация с отображением ошибок

```jsx
<Input
  label="Email"
  type="email"
  placeholder="example@theatre.ru"
  leftIcon={<Mail />}
  error={errors.email?.message}
/>
```

### Badge (Метка)

**Варианты:** `default`, `gold`, `success`, `warning`, `error`, `info`, `secondary`

**Размеры:** `sm`, `md`, `lg`

```jsx
<Badge variant="gold">Премьера</Badge>
<Badge variant="success">Активен</Badge>
```

### Alert (Уведомление)

**Варианты:** `info`, `success`, `warning`, `error`

```jsx
<Alert variant="success" title="Успешно">
  Данные сохранены
</Alert>
```

### Modal (Модальное окно)

**Размеры:** `sm`, `md`, `lg`, `xl`, `full`

```jsx
<Modal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  title="Подтверждение"
  footer={
    <>
      <Button variant="outline" onClick={onClose}>Отмена</Button>
      <Button onClick={onConfirm}>Подтвердить</Button>
    </>
  }
>
  <p>Вы уверены?</p>
</Modal>
```

---

## 🎬 Анимации

### Доступные анимации

```css
.animate-fade-in       /* Появление */
.animate-fade-in-up    /* Появление снизу */
.animate-fade-in-down  /* Появление сверху */
.animate-scale-in      /* Масштабирование */
.animate-slide-in-left /* Слайд слева */
.animate-pulse-gold    /* Пульсация золотого */
.animate-float         /* Парение */
```

### Stagger анимации

```jsx
<div className="stagger-children">
  <Card>1</Card>
  <Card>2</Card>
  <Card>3</Card>
</div>
```

### Задержки

```jsx
<div className="animate-fade-in animate-delay-200">
  Появится с задержкой 200мс
</div>
```

---

## 🛠 Утилиты

### Layout

```css
.center   /* flex items-center justify-center */
.between  /* flex items-center justify-between */
```

### Текст

```css
.text-balance        /* text-wrap: balance */
.text-gradient-gold  /* Градиентный золотой текст */
```

### Glass эффекты

```css
.glass      /* bg-white/80 backdrop-blur-md */
.glass-dark /* bg-primary/80 backdrop-blur-md */
```

### Декоративные элементы

```css
.gold-line          /* Горизонтальная золотая линия */
.gold-line-vertical /* Вертикальная золотая линия */
.pattern-curtain    /* Театральный паттерн */
```

---

## 📐 Layout

### Sidebar

- **Ширина:** 280px (CSS переменная `--sidebar-width`)
- **Фон:** Primary (#0F1419)
- **Активный элемент:** Золотой акцент с левой полоской

### Header

- **Высота:** 64px (CSS переменная `--header-height`)
- **Эффект:** Glass blur
- **Позиционирование:** Sticky

### Контент

- **Максимальная ширина:** 7xl (80rem)
- **Отступы:** 6 (1.5rem) на мобильных, 8 (2rem) на десктопе

---

## 🔧 Использование

### Подключение шрифтов

Шрифты подключаются через Google Fonts в `globals.css`:

```css
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap');
```

### Tailwind конфигурация

Все цвета, шрифты и анимации определены в `tailwind.config.js`.

### CSS переменные

Для кастомизации используйте CSS переменные в `:root`:

```css
:root {
  --color-gold: #D4A574;
  --sidebar-width: 280px;
  /* ... */
}
```

---

## 📁 Структура файлов

```
frontend/src/
├── components/
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Badge.tsx
│   │   ├── Alert.tsx
│   │   ├── Modal.tsx
│   │   ├── Spinner.tsx
│   │   └── index.ts
│   └── layout/
│       ├── Sidebar.tsx
│       ├── Header.tsx
│       └── MainLayout.tsx
├── pages/
│   ├── auth/
│   │   └── LoginPage.tsx
│   └── dashboard/
│       └── DashboardPage.tsx
└── styles/
    └── globals.css
```

---

## ✨ Рекомендации

1. **Используйте золотой акцент экономно** — он должен выделять важные элементы, а не доминировать.

2. **Заголовки в display шрифте** — используйте `font-display` для h1-h4.

3. **Тени элегантные, не агрессивные** — используйте `shadow-card` для карточек.

4. **Анимации subtile** — stagger эффекты для списков, fade-in для появления.

5. **Контраст важен** — текст всегда должен быть читаемым.

---

© 2025 Theatre Management System
